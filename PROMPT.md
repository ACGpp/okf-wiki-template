# PROMPT · 复制这段话，发给你的 AI Bot

> **使用方法**：把下方分隔线以内的全部内容复制，通过微信发给你已连接的 ZCode Bot（Bot 怎么连？看 [保姆级教程](tutorials/zcode-wechat-wiki-tutorial.md)，只需 3 步）。
> AI 会自动下载本模板、搭好目录，然后你的知识库就开张了——以后只管把公众号文章链接发给它。

---
（以下为发送内容）

请帮我从零搭建一套「公众号文章 → 个人知识库」系统。按以下规则执行：

## 1. 获取模板

- 模板仓库地址：https://github.com/ACGpp/okf-wiki-template
- 请把模板下载到临时目录（`git clone --depth 1` 或下载 zip 均可），
  将其中内容复制到 `~/Documents/okf-wiki/`。
- **不要携带 `.git` 目录**，复制完成后删除临时目录。
- `concepts/sample-*.md` 两个文件是格式示例，保留即可，我要求时再删。
- 复制完成后，请先阅读模板内的 `README.md`（约定说明书）和
  `tutorials/zcode-wechat-wiki-tutorial.md`（保姆级教程），
  之后再开始工作；有不确定的地方以这两份文档为准，不要凭空猜测。

## 2. 知识库约定

- 知识库根目录固定为 `~/Documents/okf-wiki`，独立存在，
  不要和其他项目混放，也不要动这个目录之外的文件。
- 格式遵循 Google 开源 OKF 协议 v0.2，具体字段与四段式正文约定
  以模板 `README.md` 为准。
- 我会不定时发微信公众号文章链接给你。每篇执行完整流水线：
  抓取原文 → 提炼为概念文件 → 更新 `concepts/index.md` 和 `log.md`。
  抓取时先尝试带浏览器 UA 的 curl，失败则降级用内置浏览器渲染后读取；
  文章已被删除时在 log.md 记一条例外，不要虚构内容。
- 一次发来多条链接时，启动子 agent 分批并行处理。
- 每次入库后用中文简短汇报：入了哪个文件、知识库现有条数。

## 3. 网页界面（模板自带）

- 模板已包含只读网页界面（`ui/` 目录）和构建脚本。
- 每次入库完成后，运行 `node scripts/build-index.mjs` 重建网页目录。
- 我说"看网页"时，提示我运行 `npm run dev` 后访问
  http://localhost:4173/ui/。

## 4. 重要：不要自行创建 Git 仓库

- **不要执行 `git init`、`git remote`、`git push` 等任何创建或推送
  仓库的操作。** 本条优先级最高。
- 如果我提到"备份""云端""GitHub""同步"等字眼，先主动询问我
  是否需要设置云端备份；经我确认后，再一步步引导我完成
  （建仓、SSH 密钥、关联远程等步骤见教程「进阶篇 A」）。

## 5. 开始

完成以上准备后，回复我"知识库已就绪"，并告诉我两件事：
① 模板内容是否完整；② 我现在可以怎么把第一篇文章发给你。

---

（发送内容结束）
